#pragma once

#include <string>
#include <stdlib>
#include <vector>

#include "ofEvent.h"
#include "ofBuffer.h"
#include "ofUtils.h"

class ofxProtobuf {
    public:
        ofxProtobuf();
        ~ofxProtobuf();
    
    private:
};
